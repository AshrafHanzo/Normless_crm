# CRM runs on the subdomain ops.normless.store (the store stays on normless.store).
# Node serves the frontend + API on the same origin, so this is same-origin.
VITE_API_URL=https://ops.normless.store
