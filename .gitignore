node_modules
.env
.env.local
env
shopify_token.json

# Local SQLite database + WAL files (contains real customer data — never commit)
server/crm.db
server/crm.db-journal
server/db/crm.db
server/db/crm.db-shm
server/db/crm.db-wal

# Logs
logs/
debug.log
server/debug.log

client/dist
.DS_Store
scratch_*.js
scratch_*.txt
.env.*.example

# Private setup notes (contains secrets — keep local only)
SERVER_SETUP.md

# User-uploaded design mocks / production photos (real customer files — never commit)
server/uploads/
